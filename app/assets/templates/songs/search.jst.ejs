<h1>Results for "<%= query %>"</h1>
<section id="songs"></section>
<br>
<div id="no-results">
  <% if (songs.length == 0) { %>
    Sorry, there are no results for <%= query %>... maybe try a song that doesn't suck
    <% } %>
</div>
