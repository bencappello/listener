<p><%= comment.get('author_name') %>: <%= comment.get('body') %></p>

<% if (comment.get('author_id') == Listener.currentUser.id) { %>
  <button class="btn-comment" data-id='<%= comment.id %>'>Edit</button>
  <button class="btn-comment" data-id='<%= comment.id %>'>Delete</button>
  <% } %>
